<Window x:Class="YD_RevitTools.LicenseManager.UI.AutoJoin.NearMissResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="近距離元素偵測結果"
        Height="600" Width="900"
        MinHeight="400" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- 現代化按鈕樣式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004578"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 次要按鈕樣式 -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 標題列 -->
        <Border Grid.Row="0" Background="#FF9800" Padding="20,16">
            <StackPanel>
                <TextBlock Text="⚠️ 近距離元素偵測結果" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock x:Name="tbSubtitle" Text="發現 0 組接近但未相交的元素" FontSize="12" Foreground="#FFF3E0" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- 主要內容區 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 說明文字 -->
            <Border Grid.Row="0" Background="#FFF3E0" BorderBrush="#FF9800" BorderThickness="1" CornerRadius="4" Padding="16" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="💡 以下元素彼此接近但未相交，可能需要調整輪廓以確保正確接合：" FontWeight="SemiBold" Foreground="#E65100" Margin="0,0,0,8"/>
                    <TextBlock Text="• 雙擊清單項目可以在 Revit 中選取並定位該組元素" FontSize="12" Foreground="#666"/>
                    <TextBlock Text="• 建議手動調整元素輪廓，使其相交後再執行自動接合" FontSize="12" Foreground="#666"/>
                </StackPanel>
            </Border>

            <!-- 清單 -->
            <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
                <DataGrid x:Name="dgNearMisses"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          RowHeight="40"
                          FontSize="13"
                          MouseDoubleClick="dgNearMisses_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="元素 A" Binding="{Binding ElementADisplay}" Width="2*"/>
                        <DataGridTextColumn Header="元素 B" Binding="{Binding ElementBDisplay}" Width="2*"/>
                        <DataGridTextColumn Header="距離 (mm)" Binding="{Binding DistanceMmDisplay}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="#FF6F00"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFF8F0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Border>
        </Grid>

        <!-- 底部按鈕列 -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="20,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="關閉" Width="100" Style="{StaticResource SecondaryButton}" Click="btnClose_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

